// // Standalone sauce catalog for admin management. Seeded from the sauce
// // labels already used across the customer menu (src/data/products.js) so
// // both sides agree on the same sauce ids.
// import { sauceLabels } from "./products";

// export const sauceSeed = Object.entries(sauceLabels).map(([id, name], index) => ({
//   id,
//   nameEn: name.en,
//   nameAr: name.ar,
//   icon: "droplet",
//   active: true,
//   sortOrder: index + 1,
// }));
